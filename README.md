#Project setup instructions:

Clone the repo and run composer install.

Set up the MongoDB database.

Add .env values for MongoDB configuration.

Run migrations.

Run tests (php artisan test).

API documentation generated by Swagger.